/*
* 1.什么是命名空间？
* 命名空间可以看做是一个微型模块
* 当我们把相关的业务代码写在一起，又不想污染全局的时候，我们就可以使用命名空间
* 本质就是定义一个大对象，把变量/方法/接口 都放在里面
* */

/*
* 2.命名空间和模块区别
* 在程序内部使用的代码，可以使用命名空间封装和防止全局污染
* 在程序内部外部使用的代码，可以使用模块封装和防止全局污染
* 总结：由于模块也能实现相同的功能，所以大部分情况下使用模块即可
* */