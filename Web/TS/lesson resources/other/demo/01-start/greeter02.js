// 参数添加 : string 类型注解
function greeter(person) {
    return "Hello, " + person;
}
var user = 'Jane User';
var user1 = [1, 2, 3];
// 传入一个数组
// document.body.innerHTML = greeter(user1)
document.body.innerHTML = greeter();
